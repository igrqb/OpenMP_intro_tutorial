# for intel compiler on linux (most exercises work with icc as well)
# copy to make.def
#
# Make sure to execute `. /opt/intel/oneapi/setvars.sh` in the terminal
# before executing `make test`
CC         = icx
CPP         = $(CC)
OPTFLAGS   = -openmp

CLINKER     = $(CC)
LIBS        = -lm

CFLAGS    = $(OPTFLAGS)
PRE= ./
OBJ=o
EXE=
RM=rm

